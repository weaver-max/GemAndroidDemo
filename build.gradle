plugins {
    id "com.android.application" version '9.1.0' apply false
    // 🔴 不要加 org.jetbrains.kotlin.android —— AGP 9.0 起 Kotlin 支持已内置，
    //    再应用会报 "Cannot add extension with name 'kotlin'"。
    //    仓库里 core/gemstone/tests/android/GemTest 还带着它，是尚未清理的历史写法。
    id "org.jetbrains.kotlin.plugin.compose" version '2.3.10' apply false
}
